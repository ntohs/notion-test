# 나의 첫 기술 블로그에 오신 것을 환영합니다!

안녕하세요, pjaewn 입니다. 이 문서는 GitHub Pages에서 마크다운(Markdown) 포맷이 웹페이지로 어떻게 렌더링되는지 테스트하기 위한 샘플 글입니다.

## 1. 코드 블록 테스트 (Syntax Highlighting)

기술 블로그의 핵심인 소스 코드 삽입 테스트입니다. 마크다운에서는 백틱(```) 기호를 사용해 깔끔하게 코드를 첨부할 수 있습니다. 

```javascript
// 자바스크립트 간단한 함수 예제
function greetUser(name) {
  const message = `Hello, ${name}! 블로그 개설을 축하합니다.`;
  console.log(message);
  return message;
}

```
